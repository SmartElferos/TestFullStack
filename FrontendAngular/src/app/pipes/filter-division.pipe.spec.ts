import { FilterDivisionPipe } from './filter-division.pipe';

describe('FilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDivisionPipe();
    expect(pipe).toBeTruthy();
  });
});
