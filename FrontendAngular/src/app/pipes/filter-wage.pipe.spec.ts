import { FilterWagePipe } from './filter-wage.pipe';

describe('FilterWagePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWagePipe();
    expect(pipe).toBeTruthy();
  });
});
