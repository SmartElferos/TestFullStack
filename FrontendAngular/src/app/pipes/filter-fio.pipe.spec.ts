import { FilterFioPipe } from './filter-fio.pipe';

describe('FilterFioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFioPipe();
    expect(pipe).toBeTruthy();
  });
});
